<h1>投稿一覧</h1>

<div>
  タグで探す
  <% @tag_lists.each do |list| %>
  <%= link_to list.name, people_journals_path(tag_id: list.id), class: 'tag_list'%>
  <%="(#{list.journals.count})" %>
  <% end %>
 
</div>

<% @journals.each do |journal| %>
  
  <i class="fas fa-tag"><%= journal.tags.map(&:name).join(', ') %></i>
 
  <div>
    <div>
      投稿者
      <%= journal.team.name%>
    </div>
    <div>
      画像
      <%= link_to people_journal_path(journal.id) do %>
      <%= image_tag journal.get_activity_image(100,100)%>
      <% end %>
    </div>
  
    <div>
    活動日誌
      <span>
        <%=journal.journal %>
      </span>
    </div>
    <p><%= link_to 'このユーザーの詳細へ', people_team_path(journal.team_id)%></p>
  </div>


<% end %>




